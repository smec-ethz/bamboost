[project]
name = "expdesign"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "dab-work", email = "bueelerd@ethz.ch" }
]
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["uv_build>=0.9.13,<0.10.0"]
build-backend = "uv_build"

[project]
name = "bamboost-expdesign"
version = "0.1.0"
description = "Experiment design extension for Bamboost"
readme = "README.md"
authors = [
    { name = "dab-work", email = "bueelerd@ethz.ch" }
]
requires-python = ">=3.13"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/bamboost"]
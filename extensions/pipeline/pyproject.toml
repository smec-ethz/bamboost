[project]
name = "bamboost_pipeline"
version = "0.1.0"
description = "The Pipeline extension for bamboost."
readme = "README.md"
authors = [
    { name = "dab-work", email = "bueelerd@ethz.ch" }
]
requires-python = ">=3.13"
dependencies = []

[build-system]
requires = ["uv_build>=0.9.13,<0.10.0"]
build-backend = "uv_build"
